{# @var plugin \digitalpulsebe\craftmultitranslator\DeeplTranslator #}
{# @var settings \digitalpulsebe\craftmultitranslator\models\Settings #}

{% import '_includes/forms.twig' as forms %}

{{ forms.lightswitchField({
    label: "Debug",
    instructions: "Creates log files",
    id: "debug",
    name: "debug",
    on: settings.debug,
    errors: settings.getErrors("debug"),
}) }}

{{ forms.textfield({
    label: 'Queue Jobs TTR',
    name: 'queueJobTtr',
    instructions: 'Time to reserve for the queue jobs when translating in bulk',
    type: 'number', min: 60, step: 1,
    value: settings.queueJobTtr,
}) }}
